import React from 'react';

// Composant temporairement désactivé - les alertes ne sont plus disponibles dans l'API
const AlertNotification: React.FC = () => {
  // Retourner null pour ne rien afficher
  return null;
};

export default AlertNotification;
